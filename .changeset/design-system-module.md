---
"@mgorrin/web-kit": minor
---

Añade el módulo `design-system`: `Button`, `Card`, `Input`, `Dialog` sobre `radix-ui` +
`class-variance-authority`, con tokens visuales re-temables vía `tokens.css`. Congela la superficie
pública del paquete (ver `docs/surface.md`).
